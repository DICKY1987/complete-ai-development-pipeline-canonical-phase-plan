# Pattern: preflight_verify
# Pattern ID: PAT-PREFLIGHT-VERIFY-001
# doc_id: DOC-PAT-PREFLIGHT-VERIFY-001
# Version: 1.0.0
# Category: verification

doc_id: "DOC-PAT-PREFLIGHT-VERIFY-001"
pattern_id: "PAT-PREFLIGHT-VERIFY-001"
name: "preflight_verify"
version: "1.0.0"
category: "verification"
status: "draft"
role: "spec"

schema_ref: "patterns/schemas/preflight_verify.schema.json"
executor_ref: "patterns/executors/preflight_verify_executor.ps1"
example_dir: "patterns/examples/preflight_verify/"

metadata:
  created: "2025-11-24"
  source_template: "templates\verification_templates\preflight.verify.yaml"
  category: "verification"

intent: |
  Pattern generated from template: templates\verification_templates\preflight.verify.yaml
  Provides reusable execution logic for preflight_verify operations.

inputs:
  instance_path:
    type: "string"
    required: true
    description: "Path to pattern instance JSON file"

outputs:
  execution_result:
    type: "object"
    description: "Result of pattern execution"
