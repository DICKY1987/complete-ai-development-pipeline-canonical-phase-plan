---
doc_id: DOC-GUIDE-FOLDER-LIFECYCLE-FLOW-156
---

# Pipeline Folder Lifecycle Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          ENTRY POINTS (Input Layer)                          │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    ▼                 ▼                 ▼
            ┌───────────┐     ┌───────────┐    ┌───────────┐
            │ workstreams│     │    pm/    │    │   aim/    │
            │  (specs)  │     │ (project) │    │  (tools)  │
            └─────┬─────┘     └─────┬─────┘    └─────┬─────┘
                  │                 │                 │
                  └────────┬────────┴─────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     CONFIGURATION LAYER (Support Nodes)                      │
│  ┌──────────┐  ┌──────────┐  ┌───────────┐  ┌──────────┐  ┌──────────┐   │
│  │ schema/  │  │ config/  │  │templates/ │  │  docs/   │  │registry/ │   │
│  │(contracts)│ │ (settings)│ │(patterns) │  │ (guides) │  │ (lookup) │   │
│  └────┬─────┘  └────┬─────┘  └─────┬─────┘  └────┬─────┘  └────┬─────┘   │
│       └─────────────┼──────────────┼──────────────┼─────────────┘          │
└───────────────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                      CORE DOMAIN LAYER (Processing)                          │
│                                                                               │
│  ┌────────────────────────────────────────────────────────────────┐         │
│  │                          core/                                  │         │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │         │
│  │  │orchestrator│→│ planner │→│scheduler │→│ executor │      │         │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │         │
│  │          (coordinates all domain operations)                   │         │
│  └─────────────────────────┬──────────────────────────────────────┘         │
│                            │                                                 │
│                  ┌─────────┼─────────┐                                      │
│                  ▼                   ▼                                       │
│          ┌──────────┐        ┌──────────────┐                              │
│          │  error/  │        │specifications/│                              │
│          │ (detect &│        │  (spec mgmt)  │                              │
│          │  repair) │        └──────────────┘                               │
│          └─────┬────┘                                                        │
│                │                                                             │
└────────────────┼─────────────────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    EXECUTION LAYER (Action Nodes)                            │
│                                                                               │
│  ┌──────────────────────────────────────────────────────────┐               │
│  │                        engine/                            │               │
│  │  ┌────────────┐  ┌────────────┐  ┌─────────────┐       │               │
│  │  │ adapters/  │  │   queue/   │  │orchestrator/│       │               │
│  │  │(tool bridge)│ │(task mgmt) │  │  (runner)   │       │               │
│  │  └────────────┘  └────────────┘  └─────────────┘       │               │
│  │                (executes actual work)                   │               │
│  └──────────────────────────┬───────────────────────────────┘               │
│                             │                                                │
└─────────────────────────────┼────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     INFRASTRUCTURE LAYER (Support)                           │
│                                                                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ scripts/ │  │  infra/  │  │  state/  │  │  logs/   │  │  tools/  │    │
│  │(automation)│ │(CI/CD)  │  │ (runtime)│  │(tracking)│  │(utilities)│   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
│                    (enables & observes execution)                            │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         OUTPUT LAYER (Results)                               │
│                                                                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                                  │
│  │   gui/   │  │ modules/ │  │ archive/ │                                  │
│  │(interface)│ │ (output) │  │  (done)  │                                  │
│  └──────────┘  └──────────┘  └──────────┘                                  │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Folder Roles & Dependencies

### 🎯 **Input/Entry Nodes** (Start Here)
- **workstreams/**: Task specifications → feeds core/planner
- **pm/**: Project management → creates workstreams
- **aim/**: AI tool manager → provides tool configs to engine/adapters

### 🔧 **Configuration Nodes** (Supporting Cast)
- **schema/**: Data contracts → validates all operations
- **config/**: Settings → configures core/, engine/, aim/
- **templates/**: Code patterns → used by scripts/, core/
- **docs/**: Reference guides → helps all modules
- **registry/**: Lookups → supports core/, specifications/

### 🧠 **Core Domain** (Processing Brain)
- **core/**: Orchestrates everything
  - Reads: workstreams/, config/, schema/
  - Uses: error/, specifications/, engine/
  - Writes to: state/, logs/
- **error/**: Detects & fixes issues → supports core/, engine/
- **specifications/**: Manages specs → feeds core/planner

### ⚙️ **Execution Nodes** (Workers)
- **engine/**: Executes tasks
  - Controlled by: core/orchestrator
  - Uses: aim/ (tool info), config/, schema/
  - Writes to: state/, logs/

### 🏗️ **Infrastructure Nodes** (Enablers)
- **scripts/**: Automation helpers → orchestrates migrations, validates states
- **infra/**: CI/CD → enforces quality gates
- **state/**: Runtime storage → persists engine/, core/ state
- **logs/**: Execution tracking → captures all activity
- **tools/**: Utilities → supports scripts/, engine/

### 📤 **Output Nodes** (Results)
- **gui/**: User interface → visualizes state/, logs/
- **modules/**: Generated code → output from engine/
- **archive/**: Completed work → receives from all nodes

---

## Data Flow Example

```
USER REQUEST
    │
    ▼
pm/ creates → workstreams/ws-XX.json
    │
    ▼
core/planner reads workstream
    │
    ▼
core/scheduler → engine/queue tasks
    │
    ▼
engine/adapters → aim/ (get tool configs)
    │
    ▼
engine/orchestrator executes → modules/ (output)
    │
    ├→ state/ (save progress)
    ├→ logs/ (record events)
    └→ error/ (check issues)
    │
    ▼
gui/ displays results
    │
    ▼
archive/ (when complete)
```

---

## Helper vs Worker Distinction

### 🔵 **Helper Folders** (Exist to Support Others)
- `schema/` → validates data for core/, engine/
- `config/` → configures core/, engine/, aim/
- `templates/` → provides patterns to scripts/
- `docs/` → guides all modules
- `registry/` → lookups for core/, specifications/
- `tools/` → utilities for scripts/, engine/
- `state/` → persists for core/, engine/
- `logs/` → tracks all activity
- `infra/` → enforces quality

### 🟢 **Worker Folders** (Do the Work)
- `core/` → orchestrates, plans, schedules
- `engine/` → executes tasks
- `error/` → detects & repairs
- `specifications/` → manages specs
- `scripts/` → automates operations

### 🟡 **Hybrid Folders** (Both Help & Work)
- `aim/` → manages tools (helper) + provides interfaces (worker)
- `pm/` → creates workstreams (worker) + provides context (helper)
- `workstreams/` → stores specs (helper) + defines work (worker)

---

## Critical Dependency Chains

```
1. SPEC EXECUTION CHAIN
   workstreams/ → core/planner → core/scheduler → engine/queue → engine/orchestrator → modules/

2. ERROR HANDLING CHAIN
   error/plugins → error/engine → core/orchestrator → engine/adapters

3. VALIDATION CHAIN
   schema/ → config/ → core/ → engine/ → state/

4. TOOL EXECUTION CHAIN
   aim/ → engine/adapters → engine/orchestrator → modules/

5. DOCUMENTATION CHAIN
   docs/ → all modules (reference)
```

---

## Folder Lifecycle States

```
ACTIVE (In-Progress Work)
├── core/        → Always processing
├── engine/      → Always executing
├── state/       → Always tracking
├── logs/        → Always recording
└── workstreams/ → Active tasks

STABLE (Reference/Config)
├── schema/      → Contracts
├── config/      → Settings
├── docs/        → Guides
├── templates/   → Patterns
└── registry/    → Lookups

TERMINAL (Completed)
├── modules/     → Generated output
└── archive/     → Finished work
```
