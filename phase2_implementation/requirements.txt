# Phase 2 Implementation - Requirements

# Core dependencies
pytest>=7.4.0
pytest-cov>=4.1.0

# Database (SQLite for now, can switch to PostgreSQL)
# No additional package needed - Python stdlib sqlite3

# Event logging
# Using stdlib json and logging

# Development tools
black>=23.7.0
mypy>=1.4.0
ruff>=0.0.282

# For future: Prometheus metrics
# prometheus-client>=0.17.0
