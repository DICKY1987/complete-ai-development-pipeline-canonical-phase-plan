# Router Configuration for Pipeline Plus
# Defines apps and routing policy for multi-tool task execution

apps:
  # Tool definitions with capabilities and priorities
  # Example:
  # aider:
  #   capabilities: [refactor, python, edit]
  #   priority: 1
  #   command: aider
  #   args: [--yes, --no-tty]

routing:
  # Routing policy configuration
  # default_tool: aider
  # capability_overrides:
  #   review: claude
  #   autonomous: codex
  # fallback_chain: [aider, codex, claude]
