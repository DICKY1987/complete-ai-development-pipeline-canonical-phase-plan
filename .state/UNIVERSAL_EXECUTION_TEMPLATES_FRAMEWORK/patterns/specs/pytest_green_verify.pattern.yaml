# Pattern: pytest_green_verify
# Pattern ID: PAT-PYTEST-GREEN-VERIFY-002
# doc_id: DOC-PAT-PYTEST-GREEN-VERIFY-002
# Version: 1.0.0
# Category: verification

doc_id: "DOC-PAT-PYTEST-GREEN-VERIFY-002"
pattern_id: "PAT-PYTEST-GREEN-VERIFY-002"
name: "pytest_green_verify"
version: "1.0.0"
category: "verification"
status: "draft"
role: "spec"

schema_ref: "patterns/schemas/pytest_green_verify.schema.json"
executor_ref: "patterns/executors/pytest_green_verify_executor.ps1"
example_dir: "patterns/examples/pytest_green_verify/"

metadata:
  created: "2025-11-24"
  source_template: "templates\verification_templates\pytest_green.verify.yaml"
  category: "verification"

intent: |
  Pattern generated from template: templates\verification_templates\pytest_green.verify.yaml
  Provides reusable execution logic for pytest_green_verify operations.

inputs:
  instance_path:
    type: "string"
    required: true
    description: "Path to pattern instance JSON file"

outputs:
  execution_result:
    type: "object"
    description: "Result of pattern execution"
