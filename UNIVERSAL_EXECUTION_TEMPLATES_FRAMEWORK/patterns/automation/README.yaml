name: automation
purpose: Automation utilities that detect, analyze, and triage pattern execution signals.
subdirectories:
  - name: analyzers
    purpose: Performance analysis scripts (e.g., performance_analyzer.py).
  - name: config
    purpose: Reserved for shared automation configuration and defaults.
  - name: detectors
    purpose: Anti-pattern and error detectors (execution_detector.py, file_pattern_miner.py).
usage: Run analyzers and detectors locally to surface performance issues or anti-patterns.
status: active
