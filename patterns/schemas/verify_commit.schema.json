{
  "description": "Schema for verify_commit pattern instances",
  "title": "verify_commit Pattern Instance",
  "$id": "verify_commit.schema.json",
  "properties": {
    "pattern_id": {
      "type": "string",
      "const": "PAT-VERIFY-COMMIT-001"
    },
    "doc_id": {
      "type": "string",
      "const": "DOC-PAT-VERIFY-COMMIT-SCHEMA-129",
      "pattern": "^DOC-[A-Z0-9-]+$"
    },
    "inputs": {
      "type": "object",
      "description": "Pattern-specific input parameters"
    },
    "outputs": {
      "type": "object",
      "description": "Pattern execution outputs"
    }
  },
  "required": [
    "doc_id",
    "pattern_id",
    "inputs"
  ],
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "doc_id": "DOC-PAT-VERIFY-COMMIT-SCHEMA-129"
}
